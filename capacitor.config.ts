import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.elite380.tecnificacion',
  appName: 'Elite High performance ',
  webDir: 'dist'
};

export default config;
